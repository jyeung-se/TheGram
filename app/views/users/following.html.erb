<center>
<h2><%= "#{@user.username} is following:" %></h2>

<ul>
<% @following.each do |following| %>
  <%= link_to image_tag(following.avatar.url(:real_small), class:"avater"), user_path(following) %> <br> <b><%= link_to following.username, user_path(following) %></b><br><br>
<% end %>
</ul>

</center>
